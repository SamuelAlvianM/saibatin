import { KebijakanPrivasiView } from '@/components/shared/kebijakan-privasi-view';

/**
 * Kebijakan & Privasi — konten asli diport dari app lama
 * (fronts/kebijakanprivasis). Editable via blok `info.kebijakan-privasi`.
 */
export default function KebijakanPrivasiPage() {
  return <KebijakanPrivasiView />;
}
